import TextingExperience from './TextingExperience';

export const metadata = {
  title: 'Our Lady, Texting | HAIL MARY',
  description: 'A quiet moment with Our Lady. Explore the animated sculpture in three dimensions.',
};

export default function TextingPage() {
  return <TextingExperience />;
}
